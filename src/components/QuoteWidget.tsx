
import React from 'react';
import SecureQuoteWidget from './SecureQuoteWidget';

// Legacy wrapper for backward compatibility
const QuoteWidget = () => {
  return <SecureQuoteWidget />;
};

export default QuoteWidget;
